interface:
  display_name: "vref visual references"
  short_description: "Validate and rebuild repo-local visual references"
  default_prompt: "Use $vref to validate, rebuild, or extend this repo's .vref/ visual references with structured CLI output."
